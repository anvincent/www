{# src/Core/AccountingBundle/Resources/views/Maintenance/importtransdefnshow.html.twig #}
{% extends 'CoreAccountingBundle:Default:accounting.html.twig' %}

{% block title %}Accounting - Transaction Import Definitions{% endblock %}

{% block navigation %}
    {{ parent() }}
{% endblock %}

{% block headtitle %}
Display Transaction Import Definitions
{% endblock %}

{% block body %}

{% for flashMessage in app.session.flashbag.get('returnMessage') %}
<div class="row">
	<div class="alert alert-info" role="alert">
        {{ flashMessage }}
    </div>
</div>
{% endfor %}
<div class="row">
	<div class="col-lg-12">
		<div class="panel panel-default">
			<div class="panel-heading">
				<a href="{{ path('CoreAccountingBundle_maintenance_importtransdefn_add') }}">
					<i class="fa fa-plus"></i> Add new Transaction Import Definition
				</a>
			</div>
			<!-- /.panel-heading -->
            <div class="panel-body">
            	<div class="dataTable_wrapper">
					<table class="table table-striped table-bordered table-hover" id="dataTables-example">
						<thead><tr>
							<th>Account Name</th>
							<th>Data Header</th>
							<th>Import Account Assignment</th>
							<th>Tag Assignment</th>
							<th>Edit</th>
							<th>Delete</th>
						</tr></thead>
						<tbody>
						{% for defn in importtransdefn %}
					    <tr>
					    	<td>{{ defn.accountname }}</td>
					    	<td>{{ defn.dataheaderdefn }}</td>
					    	<td>{{ defn.datalineaccount }}</td>
					    	<td>{{ defn.datalinetag }}</td>
					    	<td><a href="{{ path('CoreAccountingBundle_maintenance_importtransdefn_edit',{'importdefnid':defn.getImportdefnid}) }}">
					    		<i class="fa fa-pencil-square-o"></i>
					    	</a></td>
					    	<td><a href="{{ path('CoreAccountingBundle_maintenance_importtransdefn_delete',{'importdefnid':defn.getImportdefnid}) }}">
					    		<i class="fa fa-times"></i>
					    	</a></td>
						</tr>
					    {% else %}
					        <p>There are no tags within the definitions</p>
					    {% endfor %}
					    </tbody>
					</table>
				</div>
				<!-- /.table-responsive -->
			</div>
            <!-- /.panel-body -->
            
		</div>
        <!-- /.panel -->
	</div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
{% endblock %}

